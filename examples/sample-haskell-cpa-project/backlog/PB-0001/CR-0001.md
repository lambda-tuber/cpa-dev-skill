# CR-0001: Verify generated CPA initialization

## Status: In Progress — Requirements phase ready

---

## Current State

The sample project has been initialized from `cpa-dev-skill` templates. Shared `docs/`, `prompts/`, and `adapters/haskell/` are present locally so instruction-file references can resolve from the project root.

---

## Background

This CR exists to prove that a generated project can start CPA-compliant PB/CR iteration without relying on missing external files.

---

## Purpose

Verify that generated instructions, shared prompt entrypoints, Haskell adapter guidance, and backlog files are sufficient to begin the first CR iteration.

---

## Requirements

### Scope

- Confirm `AGENTS.md`, `CLAUDE.md`, and `.github/copilot-instructions.md` exist.
- Confirm `docs/`, `prompts/`, and `adapters/haskell/` exist.
- Confirm `prompts/pb_iteration.md` can be used as the workflow entrypoint.

### Out of Scope

- Generating Haskell source code.
- Running `cabal build` or `cabal test`.
- Opening an external AI agent session.

### CPA Layer Check

| CPA Layer | Affected? | Change Summary |
|-----------|-----------|----------------|
| CoreModel | Yes | Sample profile and validation concepts are declared. |
| ProjectedContext | Yes | Initialization flow is projected into a concrete sample project. |
| ApplicationBase | Yes | Build/test commands and tool policy are configured but not executed. |
| Interface/Boot | Yes | Agent entrypoint files are generated. |

### Completion Criteria for Requirements Phase

- [x] Generated instruction entrypoints exist.
- [x] Shared CPA assets exist locally.
- [x] PB/CR workflow files exist.

---

## Design

Not started. This sample CR stops at workflow-start validation.

---

## Development Loop

### [ ] Implementation
### [ ] Build
### [ ] Test

---

## Completion Checklist

- [ ] Human confirms the generated state is usable for CR iteration.

---
